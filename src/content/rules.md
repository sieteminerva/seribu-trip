## gallery input.json => output.json

- search meta.type
  - `node` property value is the Action type
  - `section`
    - if property is `builder`, use value to build content[]
    - else value BasicNode unwrapped it to object
